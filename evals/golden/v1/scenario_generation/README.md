# Reserved

No cases yet — the Scenario Ranker (`docs/architecture/04_AI_SYSTEM.md`
component #5) doesn't exist in ICAN 1.1. Cases here will use
`target: "scenario_generation"` per `evals/golden/schema.json` once it does.
