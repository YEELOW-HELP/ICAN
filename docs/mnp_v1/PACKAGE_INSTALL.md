# MNP V1 Package Install

The canonical full Founder-approved documentation bundle is stored in this directory as `MNP_DEVELOPMENT_PACKAGE_V1.zip`.

Most documents are also checked in as readable Markdown under `docs/mnp_v1/`.

Before implementation, if any document listed in `00_DOCUMENT_INDEX.md` is missing from the working tree, extract the ZIP locally and use the Markdown files from the bundle as the source of truth. Do not modify methodology or add scope while extracting/synchronizing documentation.

Expected full package: 44 Markdown documents.
