services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ican
      POSTGRES_PASSWORD: ican
      POSTGRES_DB: ican
    ports:
      - "5432:5432"
    volumes:
      - ican_pgdata:/var/lib/postgresql/data

volumes:
  ican_pgdata:
