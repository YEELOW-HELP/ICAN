# Reserved

No cases yet — the Opportunity Matcher (`docs/architecture/04_AI_SYSTEM.md`
component #8) doesn't exist in ICAN 1.1. Cases here will use
`target: "opportunity_matching"` per `evals/golden/schema.json` once it does.
