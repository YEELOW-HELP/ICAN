# Reserved

No cases yet — the Roadmap Planner (`docs/architecture/04_AI_SYSTEM.md`
component #7) doesn't exist in ICAN 1.1. Cases here will use
`target: "roadmap_generation"` per `evals/golden/schema.json` once it does.
