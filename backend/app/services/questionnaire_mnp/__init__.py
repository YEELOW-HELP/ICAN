"""MNP V1 -- Minimal Questionnaire (`MNP_MINIMAL_QUESTIONNAIRE_V1`).
Principle: do not ask what is already known. `missing.py` determines
which Career Capital/Career Intent fields the CareerCard still lacks;
`submit.py` writes structured answers as CLAIMED evidence. This is a
plain structured-input service layer, not an adaptive/branching
personality-test engine -- "collecting decision variables, not a
psychological test" (MNP_METHODOLOGY_V1 §11)."""
