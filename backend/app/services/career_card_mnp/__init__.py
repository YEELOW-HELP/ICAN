"""MNP V1 -- Career Card core-domain services: creation, versioning
(snapshotting), evidence recording. BLOCK B's resume parser and
questionnaire both build on top of this module rather than writing to
`MnpCareerCard`'s sub-tables directly."""
