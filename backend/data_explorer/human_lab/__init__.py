"""Human Lab (brief §15–§19): hand-author a Person Profile, hand-compare
Person ↔ Career, record a Human Expected Result, export a Golden fixture.

Everything here is authored by a human as YAML and *validated* against the
approved MNP enums — nothing is generated, inferred or AI-classified. The
schema mirrors `app/db/models_career_card.py` / `models_matching_mnp.py`;
it is a lightweight research representation, not a second Person
methodology.
"""
